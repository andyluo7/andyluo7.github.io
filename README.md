# andyluo7.github.io

Personal blog powered by Jekyll and the `minima` theme.

## Local setup

```bash
bundle install
bundle exec jekyll serve
```

Then open http://localhost:4000 to preview changes. The site will reload as you edit content in `_posts`, `index.md`, or other pages.
